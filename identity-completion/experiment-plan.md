# Diagnostic: measure natural completion of the existing identity command

The original exact command has repeatedly been killed by its5,000ms budget before returning output. A native marker experiment also timed out before its first script marker. This does not establish a Get-Process-specific problem, so no .NET replacement is adopted.

One fresh native job will run the same complete five-file prefix and existing Get-Process command, with external callback/first-byte logging only. A diagnostic-only20,000ms child ceiling permits observing natural completion; a separate non-terminating marker records when the original5,000ms deadline is crossed. No PowerShell script markers, native command rewrite, warmup, test/input/assertion change, cache change or product source edit. All236 baseline files are restored afterward and all journals retained.

This is measurement, not qualification or a shipped timeout change. If the real command completes normally beyond5seconds, the receipt can inform whether the existing Windows startup budget is undersized and what minimal bounded correction is justified. If it remains stuck, raises a real error, or this runner completes below5seconds, retain that outcome and do not declare a correction proven. Do not keep rerunning for a preferred outcome.

The previous worker's instruction to preserve5seconds was an experiment boundary, not an owner prohibition on changing internal budgets when concrete evidence justifies it. No production budget change is authorized by this diagnostic alone. Parent still owns source review, whole-fleet delivery, and all three final review gates.
